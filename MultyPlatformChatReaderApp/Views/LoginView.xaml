<UserControl x:Class="MultyPlatformChatReaderApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MultyPlatformChatReaderApp.Views"
             mc:Ignorable="d" 
             DataContext="{Binding LoginViewModel, Source={StaticResource MainLocator}}"
             d:DesignHeight="350" d:DesignWidth="800">
    <Grid>
        <TabControl MaxHeight="250">                    
            <TabItem x:Name="GoodGameLoginBlock">
                <TabItem.Header>
                    <Image Width="40" Height="40" Source="/Data/img/ggi.png" />
                </TabItem.Header>
                <TabItem.Content>
                    <ContentControl Content="{Binding GGlogin}"></ContentControl>
                </TabItem.Content>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
